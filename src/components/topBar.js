import React from 'react'
import { Text, View, SafeAreaView, Platform } from 'react-native';
import { styles } from '../../styles/style';


export default () => (
    < Text style={styles.container} > Open your app! hehesodnvhoandi</Text >
)